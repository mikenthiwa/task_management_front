import { Box } from '@mui/material';
import { ThemeToggleComponent } from '@/features/settings/appearance/components/theme-toggle.component';

export const AppearanceComponent = () => {
  return (
    <Box>
      <ThemeToggleComponent />
    </Box>
  );
};
